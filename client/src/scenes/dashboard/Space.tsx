import { Box } from "@mui/material";

const Space: React.FC = () => {
  return (
    <Box
      width="100%" // Ensures it takes the full width of the screen
      height="15px" // Sets the height of the space to 40px
    />
  );
};

export default Space;
